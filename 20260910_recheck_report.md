# 公開サイト再調査（2026-09-10 16:29 JST）

対象: https://www.piste-ai.com/  
方法: HTTPで公開170 URLを取得、HTML・XML・公開JavaScript・ローカル実装を検査。ブラウザユーズ・管理画面操作は使用していない。

## 結果

今回検査した技術項目の不足は、追加修正後0件。検索順位・AI引用率・実際のコンバージョン改善を証明する結果ではない。

| 検査項目 | 結果 |
|---|---|
| 公開URL | 170/170がHTTP200 |
| サイトマップ | 170 URL、重複0、正規URLと一致 |
| canonical | 不一致・欠落0 |
| H1 | 全ページ1件 |
| title | 重複0 |
| description | 欠落0 |
| OGP画像 | 欠落0 |
| 画像alt・幅・高さ | 属性欠落0 |
| 内部アンカー | 検査対象のリンク切れ0 |
| インデックス禁止 | 公開170 URLにnoindexなし |
| JSON-LD | 構文エラー0。法的表記3ページには未設置（必須ではない） |
| 計測コード | 全170ページに共通ローダー1件 |
| 旧URL | 検査した.html URLとindex.htmlは301で正規URLへ転送 |
| RSS・llms.txt | HTTP200。RSSは30記事を含む有効なXML |
| 存在しないURL | HTTP404 |
| 問い合わせAPI | GETは405、不正入力POSTは400。成功として扱わない |
| GAS・FunctionsのソースURL | HTTP404 |

## 再調査で見つけて補完したもの

初回巡回で、privacy・terms・tokushohoのOGP画像が未設定だった。また、ブログ一覧・6カテゴリ・法的表記3ページのロゴ画像に幅・高さがなかった。

10ページを補完し、本番反映後に170 URLを再巡回して解消を確認した。ロゴは既存の表示サイズを保つよう幅autoを併記した。今後の公開用ビルドにも `tools/complete_page_metadata.py` を組み込んだ。

追加修正のデプロイ: `6aa25c027fee69f48f3f42f1`。

## GA4・Search Console・Clarity・GTM

公開JavaScriptからGA4 `G-LR35246V7B`、Clarity `w54yutjekb`、本番ホスト限定の初期化を確認。`analytics.js` と `main.js` は公開版と検査時のローカル版が一致していた。受付成功後の `generate_lead`、LINE遷移の `line_click` を実装している。

ローカルの自動テスト4件は成功した。HTTPとコード検査は、タグが実ブラウザで発火した回数、GA4サーバー側での受信、GASからのメール配達を証明するものではない。実メールを送る試験はしていない。

前回の管理画面確認では、GA4とSearch Consoleはリンク済み、ClarityのGA連携は有効、旧 `line_friend_add` のキーイベント指定は解除済みだった。**今回は管理画面にアクセスしていないため、これらの現在状態を再確認したとは扱わない。** サイトの計測は共通Googleタグ方式であり、GTM管理設定の再確認もしていない。

## SEO・AIOで良くなった点と残る課題

155記事の著者・公開日・関連記事・カテゴリを整理し、代表紹介・支援内容・地域ページへつながる構成になっている。URL統一、サイトマップ、構造化データ、画像の軽量化は、検索エンジンや利用者が内容を取得・理解するための基盤として整った。

今後は、支援社数の集計時点や実際の成果事例、認定の参照先、最新ニュースと各サービスの仕様に対する一次資料を補強する。これらの事実確認や全記事本文の正確性を今回の機械検査で保証していない。地域ページの存在だけでGoogleマップ掲載・地域順位の改善が確認できたわけではなく、Googleビジネスプロフィールは別途実態の確認が必要。

証跡: [追加修正前](audit/20260910/recheck/summary-before-fix.json)、[追加修正後](audit/20260910/recheck/summary.json)、[全ページ検査](audit/20260910/recheck/pages.json)、[URL・API検査](audit/20260910/recheck/endpoints.json)。
